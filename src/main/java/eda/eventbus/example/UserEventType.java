package eda.eventbus.example;

/**
 * Example event type enum for user domain events
 */
public enum UserEventType {
    USER_CREATED,
    USER_UPDATED,
    USER_DELETED,
    USER_LOGIN,
    USER_LOGOUT
}